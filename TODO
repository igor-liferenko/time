send this to ub0ali:

according to that pdf file find out what this command does:

        STATUS="$(/usr/sbin/ntpq -c 'rv 0 stratum'|awk -F '=' '{ print $2 }')"

and if it can be used instead of ntpq -p | grep '^\*'

------------------------

emit "server 127.127.28.0"
emit "fudge 127.127.28.0 flag1 1"
alternatively, try -g and -G options to /sbin/ntpd

------------------

find out from dnsmasq source:
[ -f "$TIMEVALIDFILE" ] || {
	echo "ntpd says time is valid" >$TIMEVALIDFILE
	/etc/init.d/dnsmasq enabled && {
		procd_send_signal dnsmasq '*' INT
	}
}

----------------

NOTE: installing ntpd deletes /usr/sbin/ntpd, so /etc/init.d/sysntpd just becomes no-op, thus it need not be disabled
