@x
  int fd = -1;
@@y
  int fd = -1, fd2;
@@z
@y
volatile int fd = -1;
@@y
volatile int fd = -1, fd2 = -1; /* |fd2| must be set to -1 here (for |write| in signal
  handler --- if signal arrives when tty is not opened) */
@@z

@@x
  if (serial_port_opened()) write(fd, "C-------", 8);
@@y
  if (serial_port_opened()) write(fd, "C-------", 8), write(fd2, "C-------", 8);
@@z

@@x
  if (serial_port_opened()) write(fd, "A\x00------", 8);
@@y
  if (serial_port_opened()) write(fd, "A\x00------", 8), write(fd2, "A\x00------", 8);
@@z

@@x
  if (serial_port_opened()) write(fd, "A\x0F------", 8);
@@y
  if (serial_port_opened()) write(fd, "A\x0F------", 8), write(fd2, "A\x0F------", 8);
@@z
@z
